# DOKK OS

A small Node.js experiment that sends a synchronized call to phones on the same local network.

## Run

Requires Node.js 20 or newer.

```bash
npm start
```

Open the LAN address printed by the server on each device. Use a different port if needed:

```bash
npm start -- 8080
```
